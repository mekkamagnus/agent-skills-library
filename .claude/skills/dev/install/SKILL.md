---
name: install
description: "Install frontend and backend dependencies and set up environment files. Use when setting up the project for the first time, onboarding, after cloning, or when dependencies need to be installed. Triggers on: install, setup, onboard, dependencies, deps, fresh start, first time, npm install, pip install."
---

# Install & Prime

## Read and Execute
.claude/commands/prime.md

## Run
Install FE and BE dependencies
Run `./scripts/copy_dot_env.sh` to copy the .env file from the tac-2 directory.

## Report
Output the work you've just done in a concise bullet point list.